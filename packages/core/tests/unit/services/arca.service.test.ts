// This test is for the old ArcaService which no longer exists in the new architecture
// The new architecture uses Arca as a facade that composes services
// TODO: Rewrite tests for the new architecture if needed

describe.skip("ArcaService (Legacy - No longer exists)", () => {
  it.skip("legacy test - class no longer exists", () => {
    // This test suite is skipped because ArcaService no longer exists
    // The new architecture uses Arca as a facade that composes services
  });
});
